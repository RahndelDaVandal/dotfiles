# .tmux.conf

# ----------------------------------------------------------------------------------------------------
# My Config Settings
# ----------------------------------------------------------------------------------------------------
set -ga terminal-overrides ",xterm-256color:Tc"

# Change Prefix to Ctrl + a
set -g prefix2 C-a
bind C-a send-prefix -2

# -- display -------------------------------------------------------------------
set -g base-index 1           # start windows numbering at 1
setw -g pane-base-index 1     # make pane numbering consistent with windows
setw -g automatic-rename on   # rename window to reflect current program
set -g renumber-windows on    # renumber windows when a window is closed
set -g set-titles on          # set terminal title

# -- navigation ----------------------------------------------------------------
# Enable Mouse
set -g mouse on

# create session
bind C-c new-session

# split current window horizontally
bind - split-window -v
# split current window vertically
bind _ split-window -h

# pane navigation
bind -r h select-pane -L  # move left
bind -r j select-pane -D  # move down
bind -r k select-pane -U  # move up
bind -r l select-pane -R  # move right
bind > swap-pane -D       # swap current pane with the next one
bind < swap-pane -U       # swap current pane with the previous one

# pane resizing
bind -r H resize-pane -L 2
bind -r J resize-pane -D 2
bind -r K resize-pane -U 2
bind -r L resize-pane -R 2

# window navigation
unbind n
unbind p
bind -r C-h previous-window # select previous window
bind -r C-l next-window     # select next window
bind Tab last-window        # move to last active window

set-window-option -g mode-keys vi


# ----------------------------------------------------------------------------------------------------
# TPM Conifg
# ----------------------------------------------------------------------------------------------------

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
# set -g @plugin "arcticicestudio/nord-tmux"
# set -g @plugin "janoamaral/tokyo-night-tmux"
set -g @plugin 'tmux-plugins/tmux-sessionist'
set -g @plugin 'thewtex/tmux-mem-cpu-load'

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'github_username/plugin_name#branch'
# set -g @plugin 'git@github.com:user/plugin'
# set -g @plugin 'git@bitbucket.com:user/plugin'

# KeyBinds I am Testing Out
bind-key -r f run-shell "tmux neww ~/.local/bin/tmux-sessionizer"
bind r source-file ~/.tmux.conf
bind-key C command-prompt -p "window name:" "new-window; rename-window '%%'"

# ----------------------------------------------------------------------------------------------------
# STATUS BAR
# ----------------------------------------------------------------------------------------------------
# bg = "#24283b"
# bg_highlight = "#292e42"
# terminal_black = "#414868"
# fg = "#c0caf5"
# fg_dark = "#a9b1d6"
# fg_gutter = "#3b4261"
# dark3 = "#545c7e"
# comment = "#565f89"
# dark5 = "#737aa2"
# blue0 = "#3d59a1"
# blue = "#7aa2f7"
# cyan = "#7dcfff"
# blue1 = "#2ac3de"
# blue2 = "#0db9d7"
# blue5 = "#89ddff"
# blue6 = "#B4F9F8"
# blue7 = "#394b70"
# magenta = "#bb9af7"
# magenta2 = "#ff007c"
# purple = "#9d7cd8"
# orange = "#ff9e64"
# yellow = "#e0af68"
# green = "#9ece6a"
# green1 = "#73daca"
# green2 = "#41a6b5"
# teal = "#1abc9c"
# red = "#f7768e"
# red1 = "#db4b4b"
# git = { change = "#6183bb", add = "#449dab", delete = "#914c54", conflict = "#bb7a61" }
# gitSigns = { add = "#164846", change = "#394b70", delete = "#823c41" }

set -g status-style fg='#a9b1d6',bg='#414868'
set -g status-left ' '

# set -g status-right '#(~/.tmux/plugins/tmux-mem-cpu-load/tmux-mem-cpu-load)#[fg=colour233,bg=#7dcfff] %d/%m #[fg=colour233,bg=#bb9af7] %H:%M:%S '
# set -g status-right '%a, %b %d %H:%M#[fg=#a9b1d6] '
set -g status-right '%a, %b %d %H:%M#[fg=$red] '
set -g status-right-length 50
set -g status-left-length 20

# setw -g window-status-current-style 'fg=#414868 bg=#9ece6a bold'
setw -g window-status-current-style 'fg=#414868 bg=#e9af68 bold'
setw -g window-status-current-format '  #[fg=#414868]#[fg=#414868]#W#[fg=#414868]  '
# setw -g window-status-current-format ' #I#[fg=#414868]:#[fg=#414868]#W#[fg=#414868]#F '
setw -g window-status-style 'fg=#a9b1d6 bg=#414868'
# setw -g window-status-format ' #I:#W#[fg=#a9b1d6]#F '
setw -g window-status-format '  #W#[fg=#a9b1d6]  '

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
